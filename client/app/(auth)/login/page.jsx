import Login from '../../../components/common/form/login.form';

export const metadata = {
  title: 'Login | Eventora',
  description: 'Sign in to your Eventora account.',
};

export default function LoginPage() {
  return <Login />;
}
