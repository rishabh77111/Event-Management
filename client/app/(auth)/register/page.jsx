import Register from '../../../components/common/form/register.form';

export const metadata = {
  title: 'Register | Eventora',
  description: 'Create your Eventora account.',
};

export default function RegisterPage() {
  return <Register />;
}
